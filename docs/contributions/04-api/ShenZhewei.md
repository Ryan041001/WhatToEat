# API 设计与实现贡献说明

姓名：沈哲伟
学号：2312190313
日期：2026-03-27

## 我完成的工作

### 1. API 设计
- [x] 认证接口契约梳理（微信登录、登出、当前用户）
- [x] 餐厅查询接口契约整理（附近查询、关键词搜索）
- [x] 推荐接口契约设计（随机推荐、候选卡片列表）
- [x] 用户黑名单接口契约设计（创建、删除、分页查询）
- [x] 用户备注接口契约设计（创建、分页、详情、更新、删除）

### 2. 文档编写
- [x] 更新 `docs/api.md`
- [x] 更新 `docs/api.yaml`
- [x] 收口 `docs/database.md` 中与备注约束相关的描述
- [x] 收口 `docs/architecture.md`、`docs/backend.md` 中旧偏好模块残留表述
- [x] 补充本地 Docker Compose 联调说明与 `.env` 配置示例
- [x] 补充 Apifox 路径参数、环境变量和 JSONPath 提取的联调说明

### 3. 前端实现
- [ ] `frontend/src/api/` HTTP 客户端配置
- [ ] API 调用函数封装
- [ ] Mock 数据配置
- [x] 补充前后端联调所需的接口调用说明、路径参数绑定方式和 Apifox 调试指引

说明：本次个人贡献重点在 API 契约、后端实现、联调排障与测试收口，未直接提交 `frontend/src/api/` 下的独立访问层代码。

### 4. 后端实现
- [x] 完成 mock 微信登录、登出、当前用户接口
- [x] 完成附近餐厅查询与关键词搜索接口
- [x] 完成推荐接口实现，并接入黑名单过滤
- [x] 完成黑名单新增、删除、分页查询接口
- [x] 完成备注 CRUD 与内容筛选能力
- [x] 删除旧 `UserPreferenceController` 与 `UserPreferenceApplicationService` 残留，实现从统一 preference 占位接口到 blacklist / notes 资源的收口
- [x] 增加 `/api/**` 的 CORS 配置，支持浏览器和 Apifox Web 场景的预检请求
- [x] 修正 Docker / 本地开发环境下 MySQL 8 的 JDBC 连接兼容配置
- [x] 修复空 `userId` 路径段被错误包装成 `500 系统异常` 的问题

### 5. 测试
- [x] 使用 Apifox 导入和联调 OpenAPI 文档
- [x] 后端单元测试 / 集成测试
- [x] 为认证接口补充登录 / me / 登出控制器测试
- [x] 为餐厅查询与搜索接口补充控制器测试、应用服务测试，并完成编译与回归验证
- [x] 为推荐接口补充控制器测试
- [x] 为黑名单接口补充分页与稳定排序回归测试
- [x] 为备注接口补充 CRUD、筛选、参数校验与边界条件测试
- [x] 新增 OpenAPI 契约测试，校验路径参数、查询参数和成功响应结构与实现一致
- [x] 新增 CORS 预检测试，覆盖 `OPTIONS /api/v1/restaurants/nearby`
- [x] 新增空 `userId` 路径段回归测试，覆盖 `/api/v1/users//blacklist`
- [x] 测试用例数量：73 个（`./mvnw test` 全量通过时的自动化测试数量）
- [x] 多次运行后端完整测试，确认变更可回归通过

## PR 链接

- 当前课程仓库中主要在分支 `feat/api-homework-ShenZhewei-04` 上完成本次作业内容
- 本地确认的关键提交：`dbee9bc`、`1d2fcf2`、`91fdddf`、`eb70f61`
- 如课程最终要求必须提交远端 PR 链接，则需要基于当前分支补建一次 PR；当前这份说明先保留真实提交信息，不虚构不存在的 PR 链接

## 关键提交记录

- `6f19a1f`：完成认证与餐厅查询接口基础实现
- `e665bca`：新增黑名单创建接口基础实现
- `7235161`：修正鉴权校验并收缩当前 API 契约
- `5dc4e46`：完成推荐接口并同步收紧推荐 API 契约
- `eb70f61`：补全黑名单删除与分页查询接口
- `91fdddf`：完成备注 CRUD 并补齐备注 API 契约
- `1d2fcf2`：清理旧偏好接口残留并同步架构文档
- `dbee9bc`：完善 Docker 本地联调配置、修复 CORS / MySQL 连接 / 空路径异常映射，并同步收紧 API 契约文档

## 遇到的问题和解决

1. 问题：推荐接口最初只依赖一页高德结果，在过滤黑名单后容易把“还有后续可选餐厅”的场景误判成无结果。
   解决：改成按页继续拉取候选结果，在达到目标数量或上游结果耗尽前持续补足，再执行随机推荐或卡片返回。

2. 问题：黑名单和备注列表都涉及分页，如果只按时间字段排序，在相同时间戳下会出现跨页重复或漏项。
   解决：把分页排序统一收口为稳定排序，增加 `id` 作为二级排序字段，并补充 tied timestamp 回归测试。

3. 问题：备注接口的重复创建、内容校验、参数校验和文档契约容易在实现过程中发生偏移。
   解决：保留重复创建返回 `409` 的语义，通过控制器测试补齐缺字段、非法 `noteId`、空白内容、超长内容等边界场景，同时把 `docs/api.md` 和 `docs/api.yaml` 收口到与运行时一致。

4. 问题：早期设计里仍保留了统一的 preference 模块表述，但实际接口已经拆成 blacklist 和 notes 两类资源，容易误导后续开发和联调。
   解决：删除旧 `UserPreferenceApplicationService` 占位实现，并同步更新架构、后端和数据库文档，明确当前已落地的资源边界。

5. 问题：使用 Apifox Web 和浏览器调试时，后端在没有 CORS 配置的情况下会被预检请求拦住，表现为 `TypeError: Failed to fetch`，不容易第一时间定位到是跨域问题。
   解决：新增 `/api/**` 的全局 CORS 配置，并补充预检测试，确认跨域头和允许方法能够正确返回；同时把联调文档补充到 `docs/backend.md`、`docs/api.md` 和 `.env.example` 中，降低本地联调门槛。

6. 问题：Docker Compose 模式下，后端连接 MySQL 8 时出现 `Public Key Retrieval is not allowed`，导致容器不断重启，联调请求表面看起来像是接口不可用。
   解决：为 `dev` 和 `docker` 配置补充 `allowPublicKeyRetrieval=true`，并把 Docker profile、Compose 文件和本地启动说明一并收口，保证本机与容器环境都能稳定启动。

7. 问题：在 Apifox 中，如果只创建了 `userId` 环境变量但没有真正绑定到路径参数，最终请求 URL 会变成 `/api/v1/users//blacklist` 或 `/api/v1/users//notes`。早期后端会把这种非法路径错误包装成 `500 系统异常`，误导联调方向。
   解决：将 `NoResourceFoundException` 单独映射为客户端错误，并在 `docs/api.yaml`、`docs/api.md` 中明确区分 OpenAPI 的 `{userId}` 占位符与 Apifox 的 `{{userId}}` 变量替换写法，同时补充 `/api/v1/auth/me -> userId -> 用户资源接口` 的调试链路说明。

## 按作业要求还需准备的提交材料

- `git log --author=\"YOUR_NAME\" --oneline --graph` 的终端截图
- Apifox 测试集合或联调成功截图
- 前端调用 API 的界面或控制台截图
- 后端 API 响应日志或测试通过截图

说明：以上截图属于学习通提交材料，不一定都放进本 Markdown 文件，但需要和本人的提交记录、接口联调结果保持一致。

## 心得体会

这次 API 作业让我更深刻地体会到，接口开发不只是把控制器写出来，更重要的是把“实现、测试、文档、运行环境、联调方式”一起收口。尤其是在分页、鉴权、路径参数、错误码和本地 Docker 启动这些细节上，只要文档和代码稍微偏一点，联调时就会反复返工。通过这次从设计、实现、测试到文档同步，再到本地联调链路打通的完整过程，我对“先收紧契约，再补回归测试，最后把运行与调试说明补全”这套后端开发节奏有了更清晰的理解。
